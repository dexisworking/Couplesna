# Archived Firebase Hosting Config

This project no longer deploys through Firebase App Hosting.

The previous `apphosting.yaml` configuration was:

```yaml
runConfig:
  maxInstances: 5
  concurrency: 80
```

The Firebase CLI project mapping from `.firebaserc` has also been retired as part of the Supabase migration.
