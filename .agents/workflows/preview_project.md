---
description: Start the project dev server for preview
---

# Preview Project Workflow

1. Start the development server:
   ```bash
   npm run dev -- --port 3000 --hostname 0.0.0.0
   ```
2. Open a browser to `http://localhost:3000` to verify the application is running correctly.
